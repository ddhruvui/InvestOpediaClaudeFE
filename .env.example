# Copy to app/frontend/.env for local builds against a deployed API (gitignored).
# On Render, set VITE_API_BASE as an environment variable of the static site.
# Leave it unset in dev: Vite proxies /api to http://localhost:8787.
VITE_API_BASE=https://invest-opedia-claude-be.vercel.app
